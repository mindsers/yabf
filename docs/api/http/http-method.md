# HttpMethod (enum)

```ts
enum HttpMethod {
  GET = 'get',
  POST = 'post',
  PUT = 'put',
  PATCH = 'patch',
  DELETE = 'delete',
  OPTIONS = 'options',
}
```

## Description

Represent HTTP methods (verbs) supported by Yabf.
